algoritmo "exercicio_A4_1"
declare
dolar,euro,reais: real
inicio
escreva("entre com a cotacao do dólar: ")
leia(dolar)
escreva("entre com a cotacao do euro: ")
leia(euro)
escreva("entre com o valor em reais: ")
leia(reais)
escreval("valor em dolar: ",reais / dolar:6:2)
escreval("valor em euro: ",reais / euro:6:2)
fimalgoritmo



algoritmo "exercicio_A3_2"


algoritmo "exercicio_A4_3"
declare
a,b: real
inicio
escreva("entre com o primeiro termo: ")
leia(a)
escreva("entre com o segundo termo: ")
leia(b)
  escreval("( ",a," + ",b, ")" )
//escreval("(""",a," +",b," )","^2")
fimalgoritmo

algoritmo "exercicio_A4_4"
x,y,z, maior: inteiro
c1,c2,c3:caractere
inicio
escreva("entre com o nome do candidato 1: ")
leia(c1)
escreva("entre com o número de votos do candidato 1: ")
leia(x)
escreva("entre com o nome do candidato 2: ")
leia(c2)
escreva("entre com o número de votos do candidato 2: ")
leia(y)
escreva("entre com o nome do candidato 3: ")
leia(c3)
escreva("entre com o número de votos do candidato 3: ")
leia(z)
se (x <= y) e (y <= z) entao
escreval("",c3, " foi o candidato vencedor")
fimse
se (x <= z) e (z <= y) entao
escreval("",c2, " foi o candidato vencedor")
fimse
se (y <= x) e (x <= z) entao
escreval("",c3, " foi o candidato vencedor")
fimse
se (y <= z) e (z <= x) entao
escreval("",c1, " foi o candidato vencedor")
fimse
se (z <= y) e (y <= x) entao
escreval("",c1, " foi o candidato vencedor")
fimse
se (z <= x) e (x <= y) entao
escreval(" ",c2, " foi o candidato vencedor")
fimse
fimalgoritmo


algoritmo "exercicio_A4_5"
declare
cont,menor_ida,maior_ida,media_sal,cont_f,cont_idade,salario,idade: inteiro
sexo: caractere
inicio
maior_ida<-0
menor_ida<-0
media_sal <- 0
cont_idade <- 0
cont_f <- 0
              enquanto (idade >=0) faca
              escreva("entre com a idade: ")
              leia(idade)
              se (idade >=0) entao
              escreva("entre com o sexo (M) ou (F): ")
              leia(sexo)
              escreva("entre com a salario: ")
              leia(salario)
              fimse
limpatela
 se (cont = 0) entao
    maior_ida <- idade
    menor_ida <- idade
  senao
    se (idade > maior_ida) entao
    maior_ida <- idade
    se (menor_ida > idade) entao
    menor_ida <- idade
  fimse
  
  se (sexo = "f") ou(sexo = "F") entao
se (salario > 900) entao
cont_f <- cont_f + 1
fimse
fimse
fimenquanto
//maior_ida <- idade
//menor_ida <- idade
//se (idade > maior_ida) entao
//maior_ida <- idade
//senao
//se (idade < menor_ida) entao
//menor_ida <- idade
//fimse

media_sal <- idade + media_sal
cont_idade <- cont_idade + 1
escreval("média do salário dos habitantes: ",media_sal/cont_idade:2:1)
escreval("maior e menor idade dos habitantes: ")
escreval("mulheres com salário superior a R$900,00: ",cont_f)
fimalgoritmo


algoritmo "exercicio_A4_6"
declare
idade:inteiro
inicio
escreva("qual a idade da pessoa?: ")
leia(idade)
se(idade >=18) e(idade <=24)   entao
escreva("Tem um risco baixo")
senao
se(idade >24) e(idade <=40)   entao
escreva("Tem um risco médio")
senao
se(idade >40) e(idade <=70)   entao
escreva("Tem um risco alto")
senao
escreva("Não é possivel a aquisição da apólice")
fimse
fimse
fimse
fimalgoritmo
